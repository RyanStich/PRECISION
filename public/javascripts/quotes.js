//alternative to current method for saving selectef html option... pretty sure this is the worse solutuon out of the 2
// but im gonna keep it for now just in case

// function propertyType() {
//   // setting local storage for selected 'type of property'
//   let type = document.getElementById("types");

//   if (localStorage["Building Lot"]) {
//     type.value = localStorage["Building Lot"]; // set the value
//   }
//   type.onchange = function () {
//     localStorage["Building Lot"] = this.value; // change localStorage on change
//   };

//   // setting local storage for selected 'purpose of appraisal'
//   let purpose = document.getElementById("purpose");

//   if (localStorage["Capital Gains"]) {
//     purpose.value = localStorage["Capital Gains"]; // set the value
//   }
//   purpose.onchange = function () {
//     localStorage["Capital Gains"] = this.value; // change localStorage on change
//   };
// }

// let submit = document.getElementById("sub");
// submit.addEventListener("submit", propertyType());



